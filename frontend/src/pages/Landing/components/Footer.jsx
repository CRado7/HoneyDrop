const Footer = () => {
  return (
    <footer className="py-4 text-center small" style={{ color: 'var(--soft-sage)' }}>
      © {new Date().getFullYear()} Honeydrop. Made with love & honey.
    </footer>
  );
};

export default Footer;

  